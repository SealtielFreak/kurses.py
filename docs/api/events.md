# Events

`kurses.py` supports two main loop styles: a simple callback or an event-driven
runtime class.

## kurses.events

::: kurses.events
    options:
      members:
        - EventTargetRuntime
        - EmptyTargetRuntime
        - empty_target
