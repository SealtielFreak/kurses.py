# Virtual Terminal

The [`VirtualTerminal`][kurses.term.VirtualTerminal] class is the main entry
point of the library. It manages the SDL2 window, the render loop and all
subsystems.

## kurses.term

::: kurses.term
    options:
      members:
        - Rendering
        - VirtualTerminal
